# rag-agent-langgraph
project implements an advanced Retrieval-Augmented Generation (RAG) system using LangGraph to enable intelligent, self-correcting retrieval logic. The agent is capable of identifying when its initial search fails, automatically rewriting the query, and re-attempting retrieval.
